# notes-mcp 运行依赖
# 详见 docs/配置.md §5 —— 改依赖必须同步该文件(单一信息源)

# —— MCP server(协议层 + 业务层)——
fastmcp>=3.0.0                  # FastMCP:三大原语 + lifespan 建库 + 双传输(stdio/HTTP)
langchain-mcp-adapters>=0.0.4   # 消费侧 MultiServerMCPClient(agent.py 用)
langgraph>=0.2.0                # 消费侧 ReAct 状态机(agent.py 用)
langchain-openai>=0.1.0         # langchain 的 OpenAI 兼容接口(LLM 调用)
openai>=1.0.0                   # ollama 走 OpenAI 兼容 /v1 端点
python-dotenv>=1.0.0            # 读 .env

# —— 检索层(语义 + 关键词 + RRF 融合)——
chromadb>=0.5.0                 # 向量库:语义检索
bm25s[core]>=0.1.0             # BM25 关键词检索(比 rank_bm25 快 ~100x)
jieba>=0.42.0                   # 中文分词(BM25 索引/查询都要)

# —— Web 后端(FastAPI 作 MCP client,给前端 REST)——
fastapi>=0.110.0                # Web 后端框架
uvicorn[standard]>=0.27.0       # ASGI 服务器,跑 FastAPI
httpx>=0.27.0                   # 异步 HTTP 客户端(连 notes-mcp server)
